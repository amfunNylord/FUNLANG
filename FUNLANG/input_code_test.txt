FUN MAIN
CONST
    ab : int = 23;
    ds : int = 3
NOC
VAR
    a, b, c : string;
    de, l : int
RAV
BEGIN
    READ(input, letter);
    WRITE(letter);
    FOR I := 2 TO 5
    DO
      READ(input, letter);
      WRITE(letter);
    ENDFOR
    IF (A <> B) THEN
      READ(input, letter);
      WRITE(letter);  
    FI
END
NUF